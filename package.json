{
  "type": "module",
  "name": "odd",
  "version": "0.3.8",
  "scripts": {
    "repl": "tsc && clear && node --enable-source-maps dist/core/run.js",
    "test": "tsc && clear && node --enable-source-maps dist/test/run.js"
  },
  "devDependencies": {
    "@types/node": "^20.1.0",
    "typescript": "^5.0.3"
  },
  "license": "MIT"
}
