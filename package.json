{
  "type": "module",
  "name": "odd",
  "version": "0.4.0-alpha",
  "scripts": {
    "repl": "tsc && (cls || clear) && node --enable-source-maps dist/core/run.js",
    "test": "tsc && (cls || clear) && node --enable-source-maps dist/test/run.js"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  },
  "license": "MIT"
}
